<%= form_with(model: lib, local: true) do |form| %>
  <% if lib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lib.errors.count, "error") %> prohibited this lib from being saved:</h2>

      <ul>
      <% lib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number, 'Номер' %>
    <%= form.text_field :number %>
  </div>

  <div class="field">
    <%= form.label :name, 'Назавание' %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :adress, 'Адрес' %>
    <%= form.text_area :adress %>
  </div>

  <div class="actions">
    <%= form.submit 'Подтвердить' %>
  </div>
<% end %>
